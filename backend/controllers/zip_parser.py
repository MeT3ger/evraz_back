from typing import Any, Dict, List

class ZipParser:
        
    # static func
    async def parse_to_dict(data) -> List[Dict[str, Any]]:
        """Extract files, process them, and return the data models."""
        
        return {}
    async def parse_to_json(data) -> str:
        """Extract files, process them, and return the data as JSON."""
        
        return ""
    
    # and any

#          Usage
# files = ZipParser.parse_to_dict()

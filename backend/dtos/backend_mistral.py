class MistralDTO:
    
    class Requests:
        
        def zip(data):
            return {"data": data}
    
    class Responses:
        
        def zip(data): 
            return {"data": data}